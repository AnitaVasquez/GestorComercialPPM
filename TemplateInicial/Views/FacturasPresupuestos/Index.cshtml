@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<link href="//cdnjs.cloudflare.com/ajax/libs/x-editable/1.5.0/bootstrap3-editable/css/bootstrap-editable.css" rel="stylesheet" />
<script>
    $(window).on('load', function () {
        _GetCreate(null, '@Url.Action("_SeleccionCodigoCotizacion", "FacturasPresupuestos")');
        $('#contenido-modal').modal({
            'show': 'true',
            'backdrop': 'static',
            'keyboard': false
        });

        $('#contenido-modal').on('hidden.bs.modal', function () {
            debugger
            //$("#idsContactosClientes_guardar").data('api').load();
            location.href = '@Url.Action("Index","Home")';
        })
    })
</script>

@section SeccionScriptAutocomplete {
    <script src="//cdnjs.cloudflare.com/ajax/libs/x-editable/1.5.0/bootstrap3-editable/js/bootstrap-editable.min.js"></script>
}